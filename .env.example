# Backend base URL (optional)
# - If omitted in dev, Vite proxy forwards `/api/*` to http://localhost:5055 (see vite.config.js)
# - In production, set this to your deployed backend URL (Render, etc.)
VITE_API_BASE_URL=http://localhost:5055

